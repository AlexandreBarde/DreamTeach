{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="{{ asset('css/register.css') }}">

{% endblock %}


{% block body %}
<div class="container align-content-center mt-5">
    <div class="row">
        <div class="col-6 border border-secondary p-4 bg-light">
            <div class="row">
                <div class="col-5">
                    <img class="rounded-circle" src={{ asset("img/tom_bg.jpg") }} height="150px">
                </div>
                <div class="col-6">
                    <span> </br> {{ user.biography }} </span>
                </div>
            </div>
            <hr class="border-left-0 border-secondary ">
            <div class="text-center">
                </br>
                <span class="font-weight-bold h4">{{ user.Lastname }} </span>
                <span class="h4"> {{ user.firstName }} </span>
                <div> {{ userTraining.title }} | {{ schoolUser.name }} | {{ schoolUser.city }}</div>
            </div>
        </div>
        <div class="col-1"></div>


        <div class="col-5 border border-secondary p-4 bg-light">

            <div class="h4 text-center">Niveau {{ (user.xpWon / 50)|slice(0,1) }}</div>
            <div class="progress mt-3">
                <div class="progress-bar" role="progressbar" aria-valuenow="60" aria-valuemin="0"
                     aria-valuemax="100"
                     style="width:{{ ((user.xpWon)/50)|number_format(2, '.', ',')|slice(2,4) }}%;">
                </div>
            </div>
            <span class="text-center"> Vous avez {{ ((user.xpWon)/50)|number_format(2, '.', ',')|slice(2,4)/2 }} / 50 pour passer le niveau suivant</span>
            <div class="mt-4 h5 mb-4"> Badges :</div>
            {% for i in user.getBadgeid() %}
                <img src={{ asset("img/tr.png") }} height="50px"> &nbsp;
                {{ i.getName() }}
                <br/> <br/>
            {% endfor %}

        </div>


        <div class="col-6 border border-secondary p-4 bg-light mt-4">

            {% for i in noteUser %}
                <span>   {{ i.getSubjectid().getName() }} :  </span>
                {% for j in 1..i.mark %}
                    <span class="fa fa-star" style="color:orange"></span>
                {% endfor %}
                </br>
            {% endfor %}

        </div>
    </div>


    {% endblock %}
